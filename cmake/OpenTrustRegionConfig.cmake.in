# OpenTrustRegionConfig.cmake.in
@PACKAGE_INIT@

# include the exported targets file
include("${CMAKE_CURRENT_LIST_DIR}/OpenTrustRegionTargets.cmake")

# add alias (conditionally in case the package has already been included and the alias 
# has been created)
if(TARGET OpenTrustRegion::opentrustregion AND NOT TARGET OpenTrustRegion::OpenTrustRegion)
    add_library(OpenTrustRegion::OpenTrustRegion 
                ALIAS OpenTrustRegion::opentrustregion
    )
endif()
